{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist/rakesh-portfolio",
  "framework": "angular",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
